#include "main.h"

/**
 * main - C program that prints number of time the diagonal sign
 *
 * Return: Always 0.
 */
int main(void)
{
	print_diagonal(0);
	print_diagonal(2);
	print_diagonal(10);
	print_diagonal(-4);
	return (0);
}
