#include "main.h"

/**
 * mul - C function that multiply two integers
 *
 * @a: the first int used to multi in this func
 * @b: the second int used to multi in this func
 *
 * Return: a*b
 */
int mul(int a, int b)
{
	return (a * b);
}
