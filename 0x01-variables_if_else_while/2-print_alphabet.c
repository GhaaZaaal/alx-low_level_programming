#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'C program that   '
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c, d;

	d = '\n';
	for (c = 'a'; c <= 'z'; ++c)
	{
		putchar(c);
	}
	putchar(d);

	return (0);
}
